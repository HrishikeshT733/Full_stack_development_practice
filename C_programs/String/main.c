#include "Header.h"
int main() {
	char arr[SIZE];
	//accept1(arr,SIZE); //method 1
	//display1(arr, SIZE);

	//accept2(arr,SIZE); //method 2
	//display2(arr, SIZE);

	accept3(arr,SIZE); //method 3 most effective
	display3(arr,SIZE);

	_getch();
	return 0;
} 